name: arm_pre_smoke

on:
  workflow_call:
    inputs:
      task_name:
        required: true
        type: string
      image_version:
        required: true
        type: string
    outputs:
      arm_run_url:
        value: ${{jobs.JOB_compile.outputs.primary-url}}
      arm_run_key:
        value: ${{jobs.JOB_compile.outputs.primary-key}}

jobs:
  JOB_pre_smoke:
    name: Test
    steps:
      -
        name: Checkout
        identifier: process_checkout
        uses: checkout
        with:
          ref: ${{ atomgit.event.pull_request.merge_commit_sha || atomgit.sha }}
          token: ${{secrets.GIT_TOKEN}}
      -
        name: Checkout gitcode
        identifier: process_checkout_gitcode
        uses: checkout
        with:
          repository: "https://gitcode.com/cann/.gitcode.git"
          ref: master
          path: "./ci"
      -
        name: download
        uses: obs-download
        with:
          endpoint: "https://obs.cn-north-4.myhuaweicloud.com"
          bucket: "ascend-cann-open"
          access-key: ${{ secrets.AK }}
          secret-key: ${{ secrets.SK }}
          key: |
            ${{ env.obs_path }}/pr_filelist.txt
            ${{ env.obs_path }}/pr_filelist_mod.txt
            ${{ env.obs_path }}/common.sh
            ${{ env.obs_path }}/build.sh
            ${{ env.obs_path }}/ut.sh
          path: ${{ steps.process_checkout.outputs.path }}
      -
        name: tar businesscode
        run: |
          cd ${{ steps.process_checkout.outputs.path }}
          tar -zcf ../pre_smoke_data.tar.gz .
          mv ../pre_smoke_data.tar.gz ./
      -
        name: upload
        identifier: process_upload
        uses: obs-upload
        with:
          endpoint: "https://obs.cn-north-4.myhuaweicloud.com"
          bucket: "ascend-cann-open"
          access-key: ${{secrets.AK}}
          secret-key: ${{secrets.SK}}
          artifact-path: |
            ${{ steps.process_checkout.outputs.path }}/pre_smoke_data.tar.gz
          object-prefix: ${{ env.obs_ut_path }}/
      -
        name: Test
        timeout-minutes: 30
        uses: manifest-management-plugin
        with:
          action: DEPLOY
          repo: ${{ steps.process_checkout.outputs.path }}
          file_path: ${{ steps.process_checkout.outputs.path }}/ci/smoke-conf/smoke-A2.yaml
          manifest_image: swr.cn-north-4.myhuaweicloud.com/hw-ascend/${{ inputs.image_version }}
          custom_shell_script: |-
            set -ex
            export obs_path=${{env.obs_path}}
            export repo_name=${{env.repo_name}}
            export pr_id=${{ env.MERGE_ID }}
            export task_name=${{ inputs.task_name }}
            export AK=${{secrets.AK}}
            export SK=${{secrets.SK}}
            mkdir -p ~/taskspace && cd ~/taskspace
            wget -nv ${{ steps.process_upload.outputs.primary-url }}
            tar -zxf $(basename "${{ steps.process_upload.outputs.primary-url }}")
            if [ ${task_name} =  "UT_examples" ];then
              bash -x ~/taskspace/build.sh
            else
              bash -x ~/taskspace/ut.sh
            fi
      -
        name: pre_smoke resource clean
        uses: manifest-management-plugin
        with:
          action: CRUSH
          repo: ${{ steps.process_checkout.outputs.path }}
          file_path: ${{ steps.process_checkout.outputs.path }}/ci/smoke-conf/smoke-A2.yaml
    if: "${{ default() }}"
    runs-on: ['self-hosted','arch=arm','smoke=204']
